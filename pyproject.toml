[project]
name = "module-01-text-classification"
version = "0.1.0"
description = "Research project on stopword removal impact in text classification."
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
